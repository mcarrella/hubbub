import 'package:flutter/material.dart';

class GroupMap extends StatelessWidget {
	@override
	Widget build(BuildContext context) {
		return Container(
								child: Center(child: Text("group map"),
									),	
			);
	}
}